# Modal.com backend (archived)

This Python + Modal stack lived under `apps/modal-backend`. It was **replaced by Google Cloud / Vertex AI** for image generation (“Nano Banana” and related routes in the Next.js app under `/api/google/*`).

- **Archived:** March 2026
- **Code location:** [modal-backend](./modal-backend/) (FLUX / Modal `serve` + `deploy`)

Do not run `modal serve` from this archive for production; use the documented GCP + frontend env vars instead.
